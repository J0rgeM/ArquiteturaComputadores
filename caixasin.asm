#############################################################
	# This function will calculate sin(x),cos(x) and tan(x)	    #
	# It will call a function that will calculate sin(x) and    #
	# then it will call again that function but it will sum     #
	# pi/2 to calculate cos(x)				    #
	# arguments:						    #
	# f0 = x						    #
	# f1 = dx						    #
	# f2 = the number calculated to see if ends loop, term of 1 element of taylor series	    #
	# f3 = will be the sum of that will be returned in sin(x)   #
	# t1 = will put the number positive or negative             #
	# t2 = will get the result of 2*n + 1 			    #
	# return:						    #
	# void 							    #
	#############################################################
	